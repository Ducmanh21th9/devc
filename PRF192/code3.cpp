#include<stdio.h>
#include<conio.h>
int main(){
	int a,b;
	printf("nhap so a :");
	scanf("%d" ,&a);
	printf("nhap so b :");
	scanf("%d",&b);
	printf("tong cua 2 so %d va %d la %d\n", a,b,a+b);
	return 0;
}